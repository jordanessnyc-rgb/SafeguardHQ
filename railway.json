{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "echo 'worker: no build step (runs TypeScript with tsx)'"
  },
  "deploy": {
    "startCommand": "pnpm worker",
    "restartPolicyType": "ALWAYS"
  }
}
